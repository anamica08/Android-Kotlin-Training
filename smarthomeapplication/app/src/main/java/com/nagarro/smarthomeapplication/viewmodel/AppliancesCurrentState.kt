package com.nagarro.smarthomeapplication.viewmodel

//put in utils later

class AppliancesCurrentState {
    //obtain the list of objects fro, repository
    //using jsonreader and parser , add the live status properties to the objects
    // pass that list  to viewmodel so that it has update list to dipslay.
}